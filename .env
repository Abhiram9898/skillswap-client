# ⭐ BACK‑END API base (proxy fallback if vite.config.js proxy isn’t used)
VITE_API_URL=http://localhost:5000


# ⭐ Socket‑io server (if different from the REST host)
VITE_SOCKET_URL=http://localhost:5000